/** @jsxImportSource theme-ui */

import { colors } from "../../styles/theme";

const Footer = () => {
  return (
    <footer
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "200px",
        background: colors.gray200,
      }}
    >
      Footer section yet to be created
    </footer>
  );
};

export default Footer;
